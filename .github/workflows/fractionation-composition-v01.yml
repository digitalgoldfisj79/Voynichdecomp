name: fractionation-composition-v01a-closed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed:
    runs-on: ubuntu-latest
    steps:
      - name: Closed branch
        run: |
          echo 'Synthetic development gate closed after v0.1 and v0.1a specificity failures.'
          echo 'Do not run UD test or Voynich without a new preregistered programme.'
