name: VMS pure-text connections C1 canonical2

on:
  push:
    branches:
      - 'gpt56/voynich-topology-v01'
    paths:
      - 'scripts/vms_textual_connections_c1_canonical2.py'
      - '.github/workflows/vms-textual-connections-c1-canonical2.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  c1-canonical2:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check numpy requests scipy
      - name: Run frozen-body C1 calibration with Voynich connections sealed
        run: python scripts/vms_textual_connections_c1_canonical2.py
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: vms-textual-connections-c1-canonical2-v01
          path: artifacts/vms_textual_connections_c1_canonical2_v01/
          if-no-files-found: error
          retention-days: 30
