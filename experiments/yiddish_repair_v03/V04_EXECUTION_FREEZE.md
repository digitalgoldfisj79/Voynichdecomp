# Jona / Hiob external challenge v0.4 — execution re-freeze

## Retracted / superseded execution state

The earlier v0.3 Jona/Hiob challenge remains **unscored**. Its private truth/execution payload, committed by SHA256 `4dc0b6dae3c810fa121dc0c0fd169de259aca8a244a8e08911c399d59d0788bf`, was not persisted in the recoverable Supabase, GitHub Actions, repository, or Library stores inspected on 2026-09-13. It must not be regenerated and represented as that same freeze. v0.3 is therefore retired as `EXECUTION_ARTIFACT_LOST_UNSCORED`; it supplies no scientific outcome.

The v0.2 transfer failure remains a failure. The repaired v0.3 16-family result remains DEVELOPMENT evidence only and cannot qualify L or authorize Voynich/C7.

## Purpose of v0.4

v0.4 is a fresh random re-instantiation of the already frozen Jona/Hiob external challenge, required only because the unscored v0.3 private payload was lost. No v0.3 model scores existed to condition this re-freeze. The scientific instrument, source extraction, source-overlap screen, recovery thresholds, language contrast, random-map nulls, search budget, nuisance arms, and post-run oracle diagnostic are unchanged.

No Voynich target data may be loaded by this workflow.

## Pre-score reproducibility gates

Before any v0.4 challenge case is generated or scored, execution MUST reproduce all of the following:

1. Coverage-repair model input SHA256: `1bf2db4056028510a8b7f9a7eb7b168eeda20ea93f9cc090c7973091bbc87463`.
2. Jona source PDF SHA256: `0d38b1cafa59fcdc1f5328cc954b679c87429852bf56df61ff16dfcfe4a7f607`.
3. Hiob source PDF SHA256: `fb5eaee50a7e53795856019e25b9dcdc38282b96bb0a7ce60c09fd78cbaeeb79`.
4. Jona projected word count = 4119 and word-stream SHA256 = `2ded421bee57e69a6d470af2c58e1078aed7397e968e3a66f61670b9f18dd437`.
5. Hiob projected word count = 2791 and word-stream SHA256 = `2ad25c9c40a23955ea00e21c81b6419ff3bee957efa5144b3de41685318383f9`.
6. Literal and lossy-consonant 8-word overlap screens against the pinned PPCHY corpus must return zero hits for both editions.
7. The trigram solver source remains the inherited `experiments/yiddish_repair_v03/trigram.py` and `trigram.cpp`; native/Python score parity assertions must remain active.
8. Search is 32 restarts for every language model, every key, and both searched arms; 5,000 proposals/restart and 60 greedy passes are unchanged.

Failure of any pre-score gate is `V04_REPRODUCTION_FAILURE`; no challenge score may be interpreted.

## Prospective external-challenge gates

For each of the two fresh Yiddish families independently:

- correct-model recovery passes only if at least 29/32 planted keys achieve both atom recovery >= 0.90 and whole-word recovery >= 0.80;
- wrong-model selectivity fails if the German comparator reaches 29/32 recovery passes;
- deployed language decision passes only if the median independence-standardised contrast is >= +1.0.

After all blind solutions are fixed, compare returned mappings with the planted oracle under the unchanged fit objective and audit score. This is a bounding/diagnostic test only: it separates search failure from model/representation failure and licenses no rerun or threshold repair.

Both challenge families are Yiddish. The family-label permutation null is therefore degenerate and supplies no balanced-accuracy significance claim. A pass is `EXTERNAL_CHALLENGE_PASS_LIMITED`, not an L certificate. A pass still leaves the fresh matched-German panel and source/representation bridge unresolved. A failure is `EXTERNAL_CHALLENGE_FAIL` for these explicitly defined edited Romanisations only.

## Audit order

Interpret only after: circularity -> leakage -> confounds -> matched nulls -> control fairness -> measurement degeneracy -> representation dependence -> decision-rule fragility -> audit completeness.

The workflow must preserve public cases, private truth, blind solution rows, source/model manifests, code/input hashes, final result JSON, and an atomic pickle result checkpoint as separate retained artifacts.