# Decisive execution blocker — 2026-08-02

## Outcome

The target-independent synthetic smoke test passed. The initial target run then failed closed because the generic parser did not descend through the repository's `pages → line → t → ZLZI` schema. An acquisition-only adapter was added without changing the frozen panel, statistics, labels, seed, thresholds, or promotion gates.

The corrected audit resolved **40/40** proposed historical folios and detected 230 corpus folios, but nine proposed samples did not meet the preregistered minimum of 350 normalized ZLZI glyphs:

| folio | glyphs |
|---|---:|
| f5v | 233 |
| f11r | 242 |
| f14r | 336 |
| f25v | 280 |
| f28r | 346 |
| f28v | 306 |
| f32r | 349 |
| f36r | 270 |
| f41v | 326 |

Corpus SHA-256: `26e7490e099b1074ed2ce19356d0ea493aa1791826004e1c551d3f4f9bf8574f`.

## Interpretation

**MACHINE-CERTIFIED:** the current mapping/sample interpretation is incompatible with the frozen claim that every D’Imperio sample supplies 350–400 normalized glyph characters.

This has at least three possible causes, which cannot be distinguished by tuning the target analysis:

1. one or more Currier/Petersen page-number mappings are wrong;
2. D’Imperio's historical “page” sample joined units not represented by a single modern folio side;
3. historical character counting included transcription units or markup excluded by the present normalization.

## Decision

Stage 1 was **not executed** and no statistical result was promoted. Stages 2 and 3 remain **OPEN**. Lowering the 350-character integrity threshold after seeing the target lengths would violate the preregistration and is prohibited.

The next admissible step is a source-level reconstruction of D’Imperio's 40 sample units from the original table/checklist, with an explicit one-to-one provenance ledger and historical counting reproduction. That reconstruction must be frozen as v0.2 before any target statistics are inspected.
