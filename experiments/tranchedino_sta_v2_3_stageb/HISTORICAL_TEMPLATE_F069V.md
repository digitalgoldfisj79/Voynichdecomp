# Tranchedino × STA v2.3 Stage B — binding historical template

Date: 2026-08-09
Namespace: `TRANCHSTA23`
Primary historical witness: ÖNB Cod. 2398, f.69v (SYS-007)

## Purpose

Stage A tested the strict 36-sign alphabetic component of f.69v over RF full STA and closed with `NO TRANCHEDINO-STA ALPHABETIC SIGNAL`. Stage B is not a relabelled rerun. It tests the additional unit classes visibly present on the same historical key sheet.

## Binding strict inventory

The primary f.69v model contains **92 one-sign assignments**:

- **36 alphabetic homophone assignments** across 19 plaintext letters;
- **11 geminate signs**;
- **7 null signs**;
- **38 nomenclator signs**.

Two additional alphabetic cells (`a`, `c`) are uncertainty-qualified in the crop-grounded homophone audit. They are excluded from the binding strict inventory. The inclusive inventory would therefore be 94, but 94 is sensitivity-only and cannot replace K=92 after target results are opened.

### Alphabet component

Plaintext alphabet: `a b c d e f g h i l m n o p q r s t u`.

Strict multiplicities:

- `a=1`, `c=1`;
- each of `b,d,e,f,g,h,i,l,m,n,o,p,q,r,s,t,u=2`;
- total 36.

This is exactly the alphabet geometry used in Stage A.

### Geminate component

The f.69v `Gemine` row visibly supplies one sign each for:

`bb, cc, dd, ff, gg, ll, nn, pp, rr, ss, tt`

Total: **11**.

### Null component

The f.69v `Nulle` row visibly contains **7** distinct null signs.

### Nomenclator component

The lower two-column list contains **38** one-sign nomenclator assignments (19 left + 19 right). The list mixes diplomatic/entity concepts with common lexical/function entries. The Stage-B model freezes the **count and one-sign code architecture**, not the correspondent-specific semantic identities, because those identities are not a plausible Paduan pharmaceutical codebook.

## What is NOT admitted

- No syllabary is visible on f.69v and **no syllabic unit class is admitted** to the primary v2.3 model.
- No section-, quire-, page- or hand-specific free keys.
- No same-line/same-family ordering payload carrier from the previous failed programme.
- No literal EVA unitisation.
- No image-derived DINO glyph ontology.
- No post-hoc inclusion of the two uncertain alphabet cells.

A future syllabic arm would require a separately audited Cod.2398 key whose syllabary is diplomatically reconstructed before any Voynich score.

## Structural corroboration

Other inspected Cod.2398 keys independently show the same broad mixed-key anatomy (homophonic alphabet plus nulls and nomenclator; some also geminate/duplicate rows), including f.47v and f.57r. They are corroborating witnesses only. f.69v remains binding because its alphabet component is already crop-grounded and was the historical template used prospectively in Stage A.

## Scientific interpretation

The v2.3 gap is therefore narrow and explicit: can a **fixed one-sign mixed-output substitution system** with the exact f.69v class inventory (alphabetic letters, selected doubled letters, nulls, and a 38-entry word/concept codebook) be blindly recovered on fresh genuine Paduan controls and then recognised on STA without target-driven expansion of the key?
