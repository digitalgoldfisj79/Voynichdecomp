# STA boundary robustness v0.1 — closeout

Date: 2026-08-15

## Formal preregistered verdict

**REPRESENTATION_SENSITIVE**.

The formal robustness conjunction fails because the ZLZI result of no production-order direction does not survive STA-family representation:

- accretion/reduction ratio z = -2.20;
- substitution-site ratio z = -3.55.

The STA long-word ED1 arm is underpowered (37 events), so the ZLZI long-word anti-crowding result is neither replicated nor falsified here.

## What *does* survive representation change

Two core boundary-attenuation geometries survive strongly in canonical RF1b STA-family:

### Exact lag-2

- N0: 1.180x, z=3.79;
- N1: 1.073x, z=1.77;
- N3: 1.047x, z=0.72.

### Adjacent ED1

- N0: 1.102x, z=5.70;
- N1: 1.079x, z=5.16;
- N3: 1.016x, z=0.66.

Thus the substantive fact that both raw statistics are boundary/position coupled is not a ZLZI/P70 artefact.

## Held-out zero-memory adjudication

The strongest STA-native memoryless model S2 conditions independently on section, segment-length bucket, and exact edge coordinate L0-L3/R0-R3/M.

### ED1

S2 passes the preregistered F2 target:

- real N0 1.102 vs S2 median 1.054;
- real N3 1.015 vs S2 median 1.010.

Therefore the earlier broad claim that ED1 necessarily requires joint neighbouring emission is **not representation robust**. In STA-family, detailed marginal edge position is enough under the frozen tolerance.

### Exact lag-2

S2 fails F1:

- real N0 1.180 vs S2 1.064;
- real N1 1.072 vs S2 0.996;
- real N3 1.048 vs S2 0.986.

Independent exact-position emissions therefore do not reproduce the STA exact-return pattern.

## Revised programme conclusion

The earlier ZLZI/P70 statement `ED1 + lag2 imply missing joint boundary dependence` is too broad.

The representation-robust core is narrower:

> **Exact token recurrence at lag 2 has a boundary-associated excess that survives STA-family representation and is not generated by a held-out, zero-memory exact-edge positional model.**

ED1 remains a real boundary-associated statistic, but whether its residual requires pair dependence is representation-sensitive. Directionality is also representation-sensitive. Long-word ED1 is not powered in STA.

## Next justified target

Do not build a generic ED1 copy/mutation kernel.

If the programme continues, the next mechanism test should be restricted to **exact recurrence** and should distinguish:

1. richer deterministic/latent boundary templates that can generate `A ? A` without explicit token memory;
2. a minimal boundary-local return-state/identity dependency.

The first should be tested before the second. ED1 should remain a secondary diagnostic, not a fitted target.

## Evidence status

RF/STA/aaa are correlated projections of one transcription, not independent manuscript replications. This STA result upgrades representation robustness of the lag-2 boundary residual only; it does not identify language, semantics, cipher, or scribal intent.
