# Next admissible gate

The frozen codec is not yet attached to the Voynich manuscript.

Before a manuscript run, a separate versioned calibration package must add the bounded stateful morpho-local nomenclator while leaving codec v0.1 unchanged. That package must externally load and hash:

- the established morphological-house assignment;
- house-conditioned gallows admissibility;
- the frozen function-word inventory;
- section×Currier support restrictions;
- line-position categories;
- the measured adjacent-length autocorrelation target, cipher ceiling and fixed selector required to bridge the gap;
- exact physical quire/bifolium folds.

Its planted gate must extend the current policy robustness test to:

- unequal class sizes;
- mixed letter/syllable/word units;
- bounded null rates;
- global and section×Currier keys;
- held-out surface types;
- held-out plaintext documents;
- matched production-only controls preserving the same frozen surface constraints.

No Voynich comparison is admissible until every preregistered selection-policy family passes that class-specific gate.
