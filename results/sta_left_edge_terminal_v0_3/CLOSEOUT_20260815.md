# STA left-edge terminal discriminator v0.3 — closeout

Date: 2026-08-15
Formal verdict: **EXACT_LEFT_POSITION_PLUS_LINE_INVENTORY_SUFFICIENT**.
Programme status: **CLOSED**.

## Calibration

The terminal instrument calibrated cleanly.

- exact toy/convolution check: PASS;
- negative calibration: 9/2000 = 0.45% false-positive gate rate (requirement <=2%);
- positive calibration: 30/30 = 100% gate rate (requirement >=80%);
- median successful line-zone-preserving planted swaps: 36.5 (requirement >=20).

Therefore the target failure is interpretable and is not an instrument-power failure.

## Target

Primary corpus: 2,802 clean left-edge segments with n>=6, each contributing L0→L2 and L1→L3.

Cross-fitted X1 conditions on:

- each held-out line's exact START and MID token multisets;
- training-only token-specific L0/L1 preferences;
- training-only token-specific L2/L3/REST preferences;
- section and line-length bucket;
- hierarchical frozen shrinkage.

Results:

- observed total exact matches: 92;
- X1 expectation: 75.042;
- observed/expected: 1.2260;
- z = 2.389;
- exact one-sided p = 0.011306;
- primary gate: FAIL (frozen requirements z>=2.58, p<=.01, ratio>=1.10, and positive residual in both pairs).

Pair decomposition:

- L0→L2: observed 28, expected 30.030 — no positive residual;
- L1→L3: observed 64, expected 45.012 — the remaining excess is concentrated here.

The surviving signal is therefore not a general left-edge lag-2 recurrence effect. It is a narrower L1→L3 asymmetry which was not preregistered as a new primary target and has no rescue authority.

## Scientific conclusion

Across the completed sequence of tests, the initially striking boundary-associated exact-lag-2 recurrence has progressively decomposed into:

1. strong line/boundary positional structure;
2. strong line-local / zone-local token-inventory clustering;
3. exact token-position preferences at the left edge;
4. no preregistered residual that requires a manuscript-wide A?A return/copy mechanism.

The terminal X1 model does not reproduce every descriptive count exactly, but the residual fails the frozen inferential gate and one of the two left-edge component pairs is below expectation. The evidence is therefore insufficient to justify another recurrence mechanism.

The appropriate retained statement is:

> **Short-range exact-token recurrence in STA is largely a consequence of local line/zone inventory restriction plus exact positional organisation. The completed programme does not identify a distinct lag-2 return or copying process.**

This remains a statement about statistical production structure only. It does not identify semantics, language, cipher, or meaning.

## Stopping rule executed

The preregistered terminal stopping rule is now in force.

- No v0.4 recurrence rescue.
- No L1→L3 subgroup programme.
- No new copy/return parameter.
- No section/token subgroup mining to recover significance.
- Any future work on Voynich mechanism should begin from a different independently motivated question, not continue this recurrence chain.
